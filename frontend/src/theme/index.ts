import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: { main: '#2E7D32' }, // vert coton
    secondary: { main: '#FFA000' },
  },
  typography: { fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif' },
});